name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build project
      run: echo "No build step needed"

    - name: Deploy
      run: echo "Deploy step - add deployment commands if needed"
